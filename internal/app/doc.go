// Package app contains the Bubble Tea model that drives the in-terminal UI.
//
// It owns high-level state such as the active view, focused pane, layout, and
// modal flows while delegating rendering and subprocess management to the ui
// and pane packages.
package app
