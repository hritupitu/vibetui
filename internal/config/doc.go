// Package config bootstraps vibetui's on-disk configuration.
//
// It writes bundled editor and LazyGit config, resolves assistant selection,
// and returns the concrete paths consumed by the rest of the application.
package config
